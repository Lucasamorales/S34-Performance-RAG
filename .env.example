# OpenAI
OPENAI_API_KEY= "YOUR OPENAI KEY"
# Supabase Postgres
# Use the Session Pooler URI here (from Supabase UI), include ?sslmode=require
DATABASE_URL= ""

# Direct connection (optional but recommended for migrations if you add it to config)
# Use the Direct connection URI
DATABASE_DIRECT_URL= ""
# Schema (set to public)
SUPABASE_SCHEMA=public

# For upcoming Drive ingestion
DRIVE_FOLDER_ID=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=